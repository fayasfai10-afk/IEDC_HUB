import useReveal from "../hooks/useReveal";

const steps = [["01", "Discover", "Explore innovative student projects across different domains."], ["02", "Support", "Vote for ideas and help promising projects gain visibility."], ["03", "Pitch", "Submit your own startup idea and join the innovation ecosystem."]];

export default function HowItWorks() {
  const { ref, visible } = useReveal();
  return <section id="how-it-works" ref={ref} className={`relative overflow-hidden bg-warm py-20 sm:py-28 ${visible ? "is-visible" : ""}`}><div className="mx-auto max-w-7xl px-5 sm:px-8"><div className="mb-14 flex flex-col justify-between gap-6 md:flex-row md:items-end"><div className="max-w-2xl"><p className="section-label">01 / The journey</p><h2 className="section-title mt-3">From a spark to something real.</h2></div><p className="max-w-sm text-sm leading-7 text-muted">A simple path for turning early curiosity into momentum, community and measurable impact.</p></div><div className="relative grid gap-7 md:grid-cols-3 md:gap-5 md:before:absolute md:before:left-0 md:before:right-0 md:before:top-5 md:before:h-px md:before:bg-line">{steps.map(([number, title, copy], index) => <div key={number} className={`reveal relative border border-line bg-white p-6 transition duration-300 hover:-translate-y-1 hover:border-teal-brand/50 hover:shadow-[6px_7px_0_0_rgba(11,31,51,0.08)] sm:p-7 ${visible ? "is-visible" : ""}`} style={{ "--reveal-delay": `${index * 120}ms` }}><div className="relative z-10 flex items-center justify-between"><span className="grid h-10 w-10 place-items-center border-2 border-teal-brand bg-warm text-sm font-extrabold text-teal-brand">{number}</span><span className="text-xs font-bold uppercase tracking-[0.18em] text-muted">Step {index + 1}</span></div><h3 className="mt-10 text-2xl font-extrabold tracking-tight text-navy">{title}</h3><p className="mt-3 max-w-xs leading-7 text-muted">{copy}</p><div className="mt-8 flex items-center gap-2 text-xs font-extrabold uppercase tracking-[0.16em] text-teal-brand"><span className="h-1.5 w-1.5 bg-amber-brand" /> Keep moving</div></div>)}</div></div></section>;
}
